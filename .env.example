# Public website origin, used for canonical URLs, sharing images, and the sitemap.
# Replace with your domain before building for production; do not include a path.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
